// PLB-LOCAL FILE. Not present upstream, so it can never conflict on a merge.
// Prefer adding here over editing an upstream file. See docs/local-changes.md.

#ifndef PLAYERBOTS_STANDALONE_SHAREDDEFINES_H
#define PLAYERBOTS_STANDALONE_SHAREDDEFINES_H

#include "Define.h"

inline constexpr uint8 MAX_CLASSES = 12;
inline constexpr uint8 CLASS_WARRIOR = 1;
inline constexpr uint8 CLASS_PALADIN = 2;
inline constexpr uint8 CLASS_HUNTER = 3;
inline constexpr uint8 CLASS_ROGUE = 4;
inline constexpr uint8 CLASS_PRIEST = 5;
inline constexpr uint8 CLASS_DEATH_KNIGHT = 6;
inline constexpr uint8 CLASS_SHAMAN = 7;
inline constexpr uint8 CLASS_MAGE = 8;
inline constexpr uint8 CLASS_WARLOCK = 9;
inline constexpr uint8 CLASS_DRUID = 11;

#endif
